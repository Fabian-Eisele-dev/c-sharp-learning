using System;

public class Kata
{
  public static int PositiveSum(int[] arr)
  {
     int sum = 0;
    foreach (var num in arr)
    {
        if (num > 0)
            sum += num;
    }
    return sum;
  }
}
